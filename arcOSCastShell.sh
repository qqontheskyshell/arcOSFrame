#! /bin/bash
set -euo pipefail

arcOSCastShell &

arcOSCastShell=(
/mode="$1" & (visionPro & livespeak & livecaption) $mode/ &
Settings → Phone / Messages / FaceTime → Blocked Contacts from badOnKuma &
Settings → Phone / Messages / FaceTime → share with goodOnKuma &
) &