#! /bin/bash
set -euo pipefail

arcOSCastShell &

arcOSCastShell=(
/mode="$1" & (visionPro & livespeak & livecaption) $mode/ &
Settings → Phone / Messages / $iOSService → Blocked Contacts from "$badOnKuma" &
Settings → Phone / Messages / $iOSService → Sharing with a specific "$goodOnKuma" &
) &